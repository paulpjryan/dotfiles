# Copy to secrets.zsh and fill in. The real secrets.zsh is gitignored and never
# committed. Keep work/employer-specific tokens out of this PUBLIC repo — put
# those in company.zsh (also gitignored) or a private work overlay.

export GITHUB_TOKEN=""
export GH_TOKEN=""
