# General developer tooling — installed on every machine via `brew bundle`.
# Deliberately minimal: only tools that are useful regardless of company or
# project. Language runtimes, package managers, databases, and cloud/infra
# tooling are project/company decisions — put those in Brewfile.work.

brew "stow"   # dotfiles management
brew "git"    # arm64-native; /usr/bin/git's xcrun shim breaks under Rosetta
brew "gh"     # github cli
brew "rsync"
